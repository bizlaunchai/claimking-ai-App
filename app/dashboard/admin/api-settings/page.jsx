import AdminApiSettings from './AdminApiSettings.jsx';

export const metadata = {
    title: 'Admin · API Settings | ClaimKing AI',
    description: 'Manage shared API credentials used by every subscriber.',
};

const Page = () => <AdminApiSettings />;

export default Page;
