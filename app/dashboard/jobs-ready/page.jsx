import JobsReady from './JobsReady.jsx';

export const metadata = {
    title: 'Jobs Ready | ClaimKing AI',
    description: 'Publish and manage subcontractor jobs',
};

export default function Page() {
    return <JobsReady />;
}
