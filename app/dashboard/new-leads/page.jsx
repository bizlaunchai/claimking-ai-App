import NewLeads from './NewLeads.jsx';

export const metadata = {
    title: 'New Leads | ClaimKing AI',
    description: 'All incoming leads from calls (via CTM) and form submissions',
};

export default function Page() {
    return <NewLeads />;
}
